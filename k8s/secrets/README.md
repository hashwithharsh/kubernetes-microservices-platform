# Secrets

The value in `postgres-secret.yaml` is a demo value only. Do not put real passwords or cloud credentials in Git.

For a real environment I would load secrets from a proper secret-management workflow instead of committing them.
