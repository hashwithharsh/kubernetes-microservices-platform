# Voting App Source

This directory contains the Example Voting App source used as the workload for this Kubernetes project.

The Kubernetes work is kept outside this folder under `../k8s/`. I am not presenting the upstream application code as code I wrote; the focus of this repository is how I deploy and troubleshoot the workload on Kubernetes.
